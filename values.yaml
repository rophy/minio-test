minio:
  auth:
    rootUser: admin
    rootPassword: Passw0rd
  extraVolumes:
  - name: nginx-config
    configMap:
      name: configs
  extraVolumeMounts:
  - name: nginx-config
    mountPath: /opt/bitnami/nginx/conf/nginx.conf
    subPath: nginx.conf
nginx:
  service:
    type: ClusterIP
